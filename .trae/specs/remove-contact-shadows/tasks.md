# Tasks

- [x] Task 1: 移除底部的噪点阴影
  - [x] SubTask 1.1: 打开 `src/App.tsx`，找到并在 `<Suspense>` 包裹的内容中删除或注释掉 `<ContactShadows />` 组件的调用。
  - [x] SubTask 1.2: 检查并确认 `<directionalLight>` 的 `castShadow` 属性已经开启，保证正常的物理阴影依然存在。
